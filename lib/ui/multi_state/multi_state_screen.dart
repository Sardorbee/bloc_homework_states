import 'package:flutter/material.dart';

class MultiStateScreen extends StatelessWidget {
  const MultiStateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Multi State Screen'),),
    );
  }
}
