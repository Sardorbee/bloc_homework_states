import 'package:flutter/material.dart';

class SingleStateScreen extends StatelessWidget {
  const SingleStateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Single State Screen'),),
    );
  }
}
